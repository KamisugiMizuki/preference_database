# CLAUDE.md

## 项目简介
你正在协助开发一个**个人文艺作品品鉴管理程序**。该程序用于记录、整理和回顾用户欣赏过的游戏、音乐专辑、动漫、小说、影视剧等文艺作品，支持本地离线运行，并提供作品图片的网络爬取功能。

请严格按照以下需求规格进行架构设计、代码编写、功能实现与问题解答。所有未明确说明的细节，应以安全、简洁、用户体验良好为原则进行补充。

---

## 1. 作品类型支持
- 预定义类型：`游戏`、`音乐`、`动漫`、`小说`、`影视剧`
- 支持用户自定义类型（如舞台剧、综艺、纪录片等），自定义类型与其他类型享有完全相同的功能

## 2. 数据条目结构
每个作品条目是一个对象，必须包含以下字段（标注“自动”的字段由系统维护）：

| 字段          | 要求     | 说明                                    |
| ------------- | -------- | --------------------------------------- |
| 唯一ID        | 自动生成 | UUID 或自增整数，不可重复               |
| 作品名称      | 必填     | 字符串，长度 ≤ 200 字符                 |
| 作品类型      | 必选     | 从支持的类型列表中选择                  |
| 原作者/创作者 | 可选     | 字符串，便于区分版本                    |
| 个人评价等级  | 必选     | 枚举：`S` `A` `B` `C`，含义用户可自定义 |
| 个人评价文段  | 必填     | 文本，长度 10 ~ 20000 字符，支持换行    |
| 作品图片      | 可选     | 主图1张 + 附图多张，存储本地路径        |
| 品鉴日期      | 可选     | 日期，可精确到日或月                    |
| 外部链接      | 可选     | URL 列表，如豆瓣、Bangumi 等            |
| 标签/关键词   | 可选     | 多标签，如“科幻”“治愈”                  |
| 创建时间      | 自动     | ISO 时间戳                              |
| 最后修改时间  | 自动     | ISO 时间戳                              |

## 3. 核心功能需求

### 3.1 条目管理
- **新增**：需填写名称、类型、等级、评价文段；可选填其他字段；前端与后端均需校验。
- **查看**：提供列表视图和详情视图。列表视图支持卡片或表格形式，卡片显示主图、名称、等级（彩色标签）、类型、评价前50字。
- **编辑**：可修改所有字段，修改时间自动更新。
- **删除**：支持单个删除和批量删除，需二次确认。删除条目时应同步删除关联的本地图片文件（或提示用户）。
- **导出**：
  - 支持导出为 `CSV`、`JSON`、`Markdown`、`HTML` 格式。
  - 可全库导出或按当前筛选结果导出。
  - 导出时可选择是否包含图片（Base64 嵌入或打包）。
  - 支持将单条目导出为分享卡片图片。
- **导入**：支持从导出的 `JSON`/`CSV` 文件批量导入，需处理 ID 和图片路径冲突。

### 3.2 搜索、筛选与排序
- **搜索**：全字段模糊搜索，或限定于名称、标签、评价文段。
- **筛选**：按类型、等级、标签、品鉴年份等条件组合筛选。
- **排序**：按名称、等级（映射为数值）、品鉴日期、修改时间等升/降序排列。

### 3.3 作品图片爬取
- **爬取来源**：默认使用综合图片搜索引擎（Google/Bing Images），也可指定豆瓣、Bangumi 等来源。搜索关键词 = 作品名称 + 类型。
- **操作流程**：
  1. 用户可在新增/编辑界面单条触发，或在列表多选后批量触发。
  2. 系统发起请求，获取 10~30 张缩略图 URL。
  3. 以网格形式展示缩略图，每张图片可勾选。
  4. 用户勾选一张或多张确认后，下载原图至本地 `images/` 文件夹，文件名使用 `条目ID_序号` 格式。
  5. 更新条目图片路径。
- **批处理**：批量爬取时，依次弹出每个作品的选择界面。提供“使用第一张”“全部跳过”等快捷操作。
- **后备方案**：允许用户手动上传本地图片或粘贴图片 URL 直接下载。
- **合规限制**：
  - 遵守 `robots.txt` 和网站使用条款。
  - 请求间隔 ≥ 1 秒，可配置 User-Agent。
  - 不得对同一服务端造成过大压力。

## 4. 数据存储与架构
- 使用 **SQLite** 作为本地数据库，存储元数据。
- 图片文件存储在程序目录下的 `images/` 文件夹，数据库仅保存相对路径。
- 程序关闭时自动备份数据库，同时支持手动备份。
- 所有数据完全离线可用（仅爬图需要网络）。

## 5. UI/UX 设计要求
- 界面简洁，适配 Windows/macOS/Linux 桌面系统，建议使用跨平台框架（Tauri、Electron、Flutter 等）。
- 必须支持**暗色模式**。
- 主界面布局：
  - 左侧：类型、等级等快捷筛选栏。
  - 右侧：条目列表（卡片/列表切换）。
  - 顶部：搜索栏与全局操作按钮（新增、导出、导入、批量爬图）。
- 条目卡片样式：主图缩略图、作品名称、彩色等级标签、类型图标、评价文段截断。
- 详情页：大图查看，评价文段舒适排版，支持全屏浏览。
- 所有操作需有明显反馈（加载状态、成功/失败提示）。

## 6. 非功能性需求
- **性能**：支持 10000 条条目时列表滚动流畅，搜索/筛选响应 < 1 秒。
- **安全**：防止爬取功能滥用，限制请求频率；使用参数化查询防止 SQL 注入。
- **可维护性**：代码结构清晰，预留作品类型、爬取源的扩展接口。
- **国际化**：界面默认中文，但数据库和界面需支持各类语言字符。

## 7. 未来扩展规划（当前版本可不实现）
- 数据统计面板（等级分布图、时间线等）。
- 随机推荐功能。
- 社交分享（生成匿名展示页）。
- AI 辅助生成评价或评分建议。

---

## 开发指引
- 请首先设计数据库表结构和 API（或本地方法接口），然后搭建 UI 框架。
- 爬取模块应独立封装，支持切换不同的图片源驱动。
- 所有实现必须遵循上述需求，如有疑问请先询问，不自行假设。